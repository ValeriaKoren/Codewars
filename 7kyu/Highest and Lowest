function highAndLow(numbers){
    const arr = numbers.split(" ")
    let max = arr[0]
    let min = arr[0]

    for(let num of arr) {
        if(Number(num) > max) {
            max = num
        }

        if(Number(num) < min) {
            min = num
        }
    }

    return max + " " + min
}
