//Подаётся 2 числа. Нужно вывести сначала результат умножения этих чисел, потом 'x^' и второе число-1.
function derive(coefficient,exponent) {

  return (coefficient*exponent) + 'x^' + (exponent-1) //По порядку выводим то, что нам необходимо.

}
